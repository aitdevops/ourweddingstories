global:
  repoName: "us-central1-docker.pkg.dev/${GCP_PROJECT_ID}/${REPO_NAME}"
  tag: "latest"

image:
  repository: "{{ .Values.global.repoName }}/photos"
  tag: "{{ .Values.global.tag }}"
service:
  type: LoadBalancer
  port: 3001
